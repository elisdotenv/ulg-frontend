export default function GadgetsPageSlug() {
  return (
    <div className={`w-screen h-screen flex flex-col items-center justify-center`}>
      <p className={`text-white`}>This is the Gadgets slug page</p>
    </div>
  );
}
