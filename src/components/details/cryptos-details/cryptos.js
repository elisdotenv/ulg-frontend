import styles from './page.module.css';

const CryptosDetails = () => {
  return (
    <>
      <div className={`${styles.detailsWrapper}`}>This is a Cryptos Details Page</div>
    </>
  );
};

export default CryptosDetails;
